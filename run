#!/bin/bash

# Script that runs the Pythia judge inside a container that supports both Bash
# and Python 3.5.
# 
#   - stdin: run configuration (JSON)
#   - stdout: evaluation results (JSON)
#   - stderr: should be empty
#   - exit status: should be zero

DIR="$(dirname $0)"

python3 "$DIR/run.py"